#include<bits/stdc++.h>
using namespace std;
#define ll long long int 

int main(){
    ll t;
    cin>>t;
    while(t--){
        ll n,mr;
        cin>>n>>mr;
        vector<ll>ramuse(n);
        vector<ll>ramgive(n);
        for(int i=0;i<n;i++){
            cin>>ramuse[i];
        }
        for(int i=0;i<n;i++){
            cin>>ramgive[i];
        }
        
    }
}